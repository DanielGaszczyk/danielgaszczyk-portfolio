// Twitter uses the same card as Open Graph. Re-exporting keeps both in
// sync — if we ever want a Twitter-specific variant we can diverge here.
export { default, alt, size, contentType } from './opengraph-image'
